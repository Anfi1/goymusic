node_modules/
dist/
dist-electron/
*.log
.env
.vscode/
.idea/
conductor/
__pycache__/
*.pyc
venv/
.venv/
python/bin/
mockups/
browser.json
release/
docs/superpowers/
.claude/
yt_cookies.txt
*.tsbuildinfo
python/.pot_cache.json
ptf_pot.json

# Скачанные версии треков для прослушивания (python/download_versions.py)
versions/

# Android-сборка (mobile/): сгенерированный нативный проект и артефакты Gradle.
# Исходники mobile/src и конфиги -- в репозитории, всё остальное восстанавливается
# из `npx cap add android` + `gradlew assembleDebug`.
mobile/dist/
mobile/.gradle/
# Нативный проект нужен в репозитории: в нём Chaquopy-конфиг и мост к api.py,
# из чистого `npx cap add android` он не восстановится. Игнорируем только артефакты.
mobile/android/build/
mobile/android/app/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/app/src/main/assets/public/
mobile/android/app/src/main/assets/capacitor.config.json
mobile/android/capacitor-cordova-android-plugins/


# Готовый APK мобильной сборки
*.apk
